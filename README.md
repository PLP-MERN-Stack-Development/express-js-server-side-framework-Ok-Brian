# 🧩 Express Products API

A RESTful API built with Express.js that supports CRUD operations, middleware, and error handling.

## 🚀 Setup

```bash
npm init -y
npm install express body-parser uuid dotenv
